# dependencies
/node_modules

# next.js
/.next/
/out/
# auto-generated by `next dev`/`next build` on every version change — never edit, never commit
/next-env.d.ts

# production
/build

# runtime user uploads (admin media library — user content, not source)
/public/images/uploads/

# misc
.DS_Store
Thumbs.db
Desktop.ini
*.pem
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# env files (keep the example)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE / editors
.idea/
.vscode/

# AI tooling — locally installed skills and their lock files
.agents/
.claude/
skills-lock.json

# vercel
.vercel
